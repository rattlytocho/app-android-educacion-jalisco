<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="0dp"
    tools:context="mx.gob.jalisco.portalsej.portalsej.Intro$PlaceholderFragment">


    <ImageView
        android:id="@+id/section_img"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:foregroundGravity="center"
        android:layout_above="@+id/textSlide"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:src="@drawable/indicator_unselected"
        android:layout_marginBottom="28dp" />

    <TextView
        android:id="@+id/titleSlide"
        android:layout_width="wrap_content"
        style="@style/Base.TextAppearance.AppCompat.Title"
        android:text="Titutlo"
        android:textColor="@color/colorPrimaryDark"
        android:layout_height="wrap_content"
        android:layout_below="@+id/section_img"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/section_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Topop"
        android:textAlignment="center"
        style="@style/Base.TextAppearance.AppCompat.Title"
        android:textSize="18sp"
        android:textColor="@color/colorPrimary"
        android:layout_below="@+id/titleSlide"
        android:layout_alignStart="@+id/section_img"
        android:layout_marginTop="22dp"
        android:layout_alignEnd="@+id/section_img" />

</RelativeLayout>
